#include <stdio.h>
void main()
{
    int a;
    float area;
    printf("Enter the radius of circle values");
    scanf('%d', &a);
    area = 3.14 * a * a;
    printf("area of circle is %f", area);
    
}