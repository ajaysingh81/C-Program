#include<stdio.h>
#include<math.h>
// in this program i will learn type conversion;

int main(){
//     int n ;
//     n = 5;
//   
//     printf("%d",n);


    // int num = 4;
    // // in this line  i want to find the avarage of these number but we know that datatype of denominator is int so here we use typer conversion concept because if we are not change the data type that time which ouput i want ,not find using this program that the reason we need to change the data tpye of denominator expilicitally . 
    // float avg = (54+56+56+45)/(float)num;

    // printf("%f",avg);

    // here we use floor keyword

    float k =4.4;
    
    printf("%d",floor(k));   // explore floor(minimum nearest
    
    printf("%f",ceil(k));    // explore ceil (greatest nearest)


    return 0;
}